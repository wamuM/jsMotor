/** A sprite that is drawed in the canvas*/
class Sprite{
    /**
     * 
     * @param {*} img 
     * @param {Number} x relative x cord
     * @param {Number} y relative y cord
     * @param {Number} r relative rotation in radians 
     * 
     */
    constructor(img,x,y,r){
            this.rad = img
            this.x = x;
            this.y = y;
            this.r = r;
    }
}